# YouTube Video Search - React App
Small React App which uses YouTube API to search videos.
## Install
    $ git clone git@github.com:TomePale/youtube-video-search.git
    $ cd PROJECT
    $ npm install
    $ npm run dev
Runs the app in the development mode.
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.